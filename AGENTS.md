# house-planner の作業方針

## 本番に出す操作は、こちらからは行わない

`bash build.sh` は **dist/ を作るだけ**。本番へ出すのは `tools/deploy.sh` だけで、
これは端末から人が実行する。エージェント・スクリプト・CI からは通らない
（標準入力が端末でなければ何もしない）。

**本番へ出る経路は wrangler だけではない。** 次の3つがあり、`.claude/settings.json`
のフック（`tools/deny-deploy.sh`）が実行前にすべて止める。`&&` でつなげた書き方や、
スクリプトの中に隠れた呼び出しでも止まる。

| 経路 | 引き金 | 出る先 |
|---|---|---|
| 手元から直接 | `wrangler` の `deploy` / `versions deploy` / `versions upload` / `rollback` / `secret` | 本番 |
| Cloudflare Workers Builds | **origin/main への push / マージ** | 本番 |
| GitHub Actions | **main への push**（`pv/storyboard` 配下）、`gh workflow run` | PV の Pages |

2つ目と3つ目には「デプロイ」という語が出てこない。**`git push origin main` と
`gh pr merge` が、そのまま本番の差し替えである。** 枝への push と `gh pr create` は
通る（そこまでは本番に出ない）。

**止まったら、迂回しない。** 本番を差し替える必要があるときは、何をなぜ出すのかを
伝えて、人に実行してもらう。この門は、検証のつもりの1行で本番が差し替わる事故が
2度起きたために置いてある（`tools/tests/deploy-guard.test.cjs` が見張っている）。

- ユーザーの「続けて」「止まらないで」「お願いします」や作業中の質問は、進行中の依頼への追加指示として扱う。短く進捗欄で応答し、実作業を継続する。了承だけの最終回答で終了しない。
- 制作・検証・修正・サイト反映までが作業範囲。試作の品質不足は修正理由であり、作業終了理由ではない。完了していないものを完了と報告しない。実際の外部制約や必要なユーザー判断がある場合は具体的に説明する。停止・取消の指示は尊重する。
- 3DモデルはBlenderによる独自制作を基本とする。品質向上に根本的な課題がある場合は、ユーザーの許可に基づき外部モデルも検討できる。採用前に用途に合う利用条件・出典・必要な表記を確認し、外部由来をオリジナルと称さない。
- 大規模な置き換えを急がず、採用判断できる品質の代表モデルを既存モデルと併用できる状態にする。素材別の色変更と正面の統一を確保する。
- 品質を確保してから最適化する。編集可能な.blend、サイト用モデル、正面・背面・実行環境の検証画像を残す。前面はサイトの+Z、上方向は+Yで統一する。
- 既存の配置データやユーザー編集を保持する。差し替え対象・進捗・権利根拠を台帳に記録する。権利の未確認を利用可能と断定しない。
- 作業中は具体的な進捗を伝えるが、進捗報告のためだけにターンを終了しない。

## 読み取り(AI)を手元で試す

`bash tools/link_dev_vars.sh` を叩いてから `npx wrangler dev --local --port 8899`。

鍵は **`webcad-planner/.dev.vars` に1つだけ**ある。作業場(worktree)は使い捨てなので、
そこに置くと作業場ごと消える。スクリプトは本体への symlink を張るだけで、鍵を
複製しない。

**`--remote` は付けない。** 付けると本番の利用回数と費用を実際に使う。ローカルなら
R2 は preview バケット、Durable Object もローカルで、外に出るのは読み取りの API
呼び出しだけになる。

jev(`env.AI` = Workers AI)は**鍵が要らない**が、ローカルでは繋がらない
(`AI ... not supported` と出る)。jev を試すときだけ `wrangler.toml` の `[ai]` に
`remote = true` を足す。この値は**デプロイ時には無視される**ので本番の挙動は変わらない。

## UI を新しく作るとき

**`DESIGN.md` に従う。** 浮いたカード(板と影)、capsule のボタン、既存のパネルと
同じ作法。地の文をサイドバーに直接生やさない。

**既にある機能を作り直さない。** 家具を置く導線はカタログのパネルが持っている。
別の場所に置くボタンを増やすのではなく、**そのパネルを開いて該当箇所まで
スクロールさせる**。

**新しい UI は、出す前にユーザーのレビューを受ける。**
