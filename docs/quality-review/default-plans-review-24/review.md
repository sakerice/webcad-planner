# plan (24) ユーザー編集のコミット

ユーザー指定の修正版を内容変更なしで2階建てデフォルトに採用。キッチンの配置・窓・空調、ホールと建具、家具、車の向きと色、外観設定・表示位置を含め、受領JSONの全データを保持。3階建ては変更なし。

`user.json` は受領原本。再生成時は review 23 の後に `tools/default_plan_2f_review_24.json` を適用する。

今回は修正ループではなくコミットの依頼なので、配置検査の指摘は自動修正していない。扉と壁の干渉、ホール側の扉前の幅、室の20mm重複、壁端の60mmずれ、キッチン通路・作業距離、窓上端、収納率の計9件を検出。詳細は `lint.txt`。既存の品質回帰は前回の配置基準を含むため、現版の品質合格とは扱わない。
