# 初回選択の外観サムネイル

現在の2階建て（plan 24）・3階建てのWebGL描画から960×640 JPEGを作成。編集UIと隣家を除き、南東方向から建物・外構を撮影。生成画像や別物件の写真は使用していない。

`tools/capture_preset_thumbnails.cjs` で再撮影可能。各画像は `assets/presets/` に配置。デスクトップは2列、モバイルは1列。画像全体を含むカードから選択可能。

1280×900と390×844で、2画像の読み込み、横はみ出しなし、2階建て・3階建ての選択とダイアログの終了をブラウザで確認。
