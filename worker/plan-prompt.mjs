// 間取り図を読ませるときの指示文。
//
// ここだけ独立させてある理由
// --------------------------
// 読み取りの精度は、ほぼこの文章で決まる。モデルや通信の都合とは別の速さで
// 何度も書き直すことになるので、呼び出し側 (worker/routes-ai.mjs) と
// 分けてある。検査もこのファイルだけを読む。
//
// この文章が担うもの / 担わないもの
// ----------------------------------
// **形は担わない。** 出力のJSONの形と、項目ひとつずつの意味は
// worker/plan-response-schema.mjs にあり、通信の設定(responseSchema)として
// APIに強制させる。ここに日本語で書き写すと二重になり、いずれ食い違う。
//
// ここが担うのは **読み取りの段取り** だけ。何を先に決め、何をその次に
// 決めるか。図面を読む順番そのものが精度を決めるので、そこに集中させる。
//
// 何を読ませて、何を読ませないか
// ------------------------------
// 読ませるのは 壁・建具・階段・室名・水まわりの設備。動かせる家具
// (ベッド・ソファ・食卓)は読ませない。図面の家具記号はそのメーカーの
// 標準仕様の絵であって、実際に置く物とは別物だし、アプリには家具の
// カタログがある。無理に読ませると量が増えて高くなり、肝心の壁の精度が落ちる。
//
// 座標の約束はアプリと同じ。単位はミリメートル、xは右、yは下が正。
import { ALLOWED_ITEM_TYPES, itemTypeTable } from "./plan-item-spec.mjs";

export { ALLOWED_ITEM_TYPES };

export const SYSTEM_PROMPT = [
  "あなたは日本の住宅の間取り図を読み取って、住宅設計アプリが読み込めるデータにする専門家です。",
  "図に描かれていることと、そこから確実に導けることだけを答えます。ありそうだからという理由で足しません。",
  "出力はJSONだけです。説明文や前置きは書きません。",
].join("\n");

// 図の説明(利用者が書いた補足)があれば hint として渡す。
export function buildPlanPrompt({ hint } = {}) {
  return `この画像は日本の住宅の間取り図です。これを住宅設計アプリに取り込める形に起こしてください。

# 1. このアプリで家がどう出来ているか

家は次の層で組み立てられます。**あなたが読むのは、図面にしか無いものだけです。**

| 層 | 何か | 誰が作るか |
|---|---|---|
| 敷地 | 土地の範囲 | 平面図には描かれていないので **作らない** |
| 基礎 | 建物が載る土台 | **アプリが壁の外形から自動で作る。出力しない** |
| 壁 | 外周と間仕切り。芯線で持つ | **あなたが読む** |
| 部屋 | 壁で囲まれた領域 | **アプリが壁から計算する。出力しない** |
| 建具 | 窓・扉。壁の上に載る | **あなたが読む** |
| 階段 | 階をつなぐ | **あなたが読む** |
| 設備 | 浴槽・便器・洗面台・流し台 | **あなたが読む** |
| 屋根 | 最上階の上に載る | **アプリが壁の外形から自動で作る。出力しない** |

ここから分かる大事なこと:

- **部屋はあなたが作るのではなく、あなたが読んだ壁から計算されます。**
  間仕切り壁を1本落とすと、2つの部屋がつながって1つになります。
  **壁の精度が、そのまま家の精度です。**
- 室名は「どこに何と書いてあるか」だけを答えてください。範囲は要りません。
  計算した部屋のうち、その文字を含むものがその名前になります。

# 2. 座標の約束

- 単位はミリメートル。x は右が正、y は下が正。
- **建物の左上の外角を (0,0) とします。** 負の値は使いません。
- 壁は**芯(中心線)**で表します。外形の寸法線(例 7,280)は芯々の寸法です。
- 建具・設備の x,y は**その物の中心**です。左上の角ではありません。

## 全階で座標系はひとつ（いちばん間違えやすいところ）

各階を紙の上に並んでいる位置のまま出してはいけません。

  誤り: 1階 y=0..4095、2階 y=4095..8190（紙の上下に並んでいたのでそのまま積んだ）
  正解: 1階 y=0..4095、2階 y=0..4095（同じ建物なので重ねる）

**建物の同じ角を、全階で同じ座標にします。** 上の階の床は下の階の壁に載るので、
重ねたときに外形がそろうはずです。そろわないなら、どこかの階の原点がずれています。

# 3. 日本の住宅の寸法の決まり

- ほとんどの寸法は **227.5mm（4分の1間）の倍数**で、とくに 455 の倍数が多い。
  910・1365・1820・2730・3185・4095 などが並びます。
- **227.5 や 1137.5 のような「.5」で終わる値も正常**です。整数に丸めたり
  読み飛ばしたりしないこと。
- 図面に書かれた寸法線の数値が最優先。数値と見た目が食い違うときは数値に従う。
- 1帖 = 910×1820。「洋室(4.5帖)」のような表記は面積の裏取りに使えます。

# 4. 読み取れないところの扱い（ここを取り違えないこと）

図が粗くて読めない箇所は必ず出てきます。そのとき:

**補ってよい（他の情報から確実に決まるもの）**
- 内訳の合計が総寸法に足りず、**欠けているのが1つだけ**なら、その値は差で決まる。
- 潰れて読めない寸法が、両隣の通り芯の位置から一意に決まる。
- 上階の外形が、下階の外形と一致することから決まる。
- 階段の位置が、下の階の階段と上下で重なることから決まる。

**補ってはいけない（図に根拠が無いもの）**
- 読めない室名を「ホール」「その他」などと書く。
- 描かれていない窓や扉を、ふつう在るからという理由で足す。
- 標準的な寸法だからという理由で、読めない寸法を決める。
- 見えない部分の間仕切りを、ありそうな形で引く。

**補ったものは必ず notes に「何を、何から、どう決めたか」を1行で書いてください。**
決められないものは出さずに省き、省いたことを notes に書いてください。

# 5. 手順

## 手順0（全体）

図の中に平面図が何枚あるかを数え、それぞれが何階かを見出し(「1階平面図」など)
で判断します。**平面図だけを読みます。** 俯瞰図・立面図・外構図・表題欄は無視。

以下の手順1〜9を、**階の数だけくり返します。** 1階から順に進めてください。

## 手順1. 寸法線を読む → dims

上下左右の4辺それぞれについて、いちばん外側の「総寸法」と、その内側の
「内訳の並び」を読みます。並びの順は、横の辺なら左から、縦の辺なら上からです。

## 手順2. 検算する（1回だけ）→ dims を確定

内訳の合計が総寸法と一致するかを確かめます。合わないときは、次のよくある
読み違いに当てはまらないかを見て、**一度だけ**直します。

  - 227.5 を 2275 と読む（桁がひとつ違う）
  - 1137.5 の「.5」を落とす
  - 隣り合う2つの数字をつなげて1つに読む
  - 内訳を1つ読み落としている（差が1つぶんなら、手順4の規則で補える）

それでも合わなければ**そこで打ち切り**、総寸法を正として先へ進みます。合わない
内訳はその辺では使わず、総寸法だけを使います。**ここで何度も読み直さないこと。**

## 手順3. 通り芯を決める

検算の通った内訳を左から（縦なら上から）足していけば、通り芯の座標が出ます。
上下の寸法線が別々の通りを指していることがあるので、両方使ってください。
この階で使える座標は、ここで出た値だけです。

## 手順4. 階段を置く → items

**階段を先に置きます。** 階段は階をつなぐので、上下階で位置が合っていないと
家として成り立ちません。ここがずれると建物全体がずれます。

- 1階: 図の階段（段を描いた細長い部分、「UP」の文字、段の番号）を探して置く。
- 2階以上: **1階で決めた階段と、平面上で同じ位置**に来るはずです。ずれていたら
  この階の原点がずれています。手順3に戻って座標を合わせ直してください。
- 階段が無い階（最上階など）は飛ばします。

## 手順5. 外周の壁を置く → walls

手順3の通り芯に沿って、建物の外周を閉じた輪になるように引きます。
角では端点の座標を完全に一致させてください。

## 手順6. 部屋の区画を目で捉え、その境界を壁にする → walls

**まず、この階にいくつ部屋があり、それぞれどこからどこまでかを目で捉えます。**
（浴室・トイレ・洗面・収納のような小さい部屋も1つと数えます）

次に、**捉えたすべての部屋の輪郭を、壁として引きます。** 部屋は壁から計算される
ので、輪郭の一辺でも壁が無いと、その部屋は隣とつながって消えます。

- 建具（扉・引戸）の位置で壁が途切れて描かれていても、**壁はつなげて引きます。**
  途切れさせると2つの部屋が1つになります。建具は次の手順で別に置きます。
- 手順3で出した通り芯から外れた位置に壁を置かないでください。

## 手順7. 室名を拾う → labels

図に書かれている室名と、その文字の位置を出します。**範囲は出しません。**
読めない文字は出さないでください（手順4の「補ってはいけない」に当たります）。

手順6で数えた部屋の数と、室名の数を見比べてください。名前の無い部屋
（廊下・ホールなど、図にも書かれていない）はそのままで構いません。
逆に**室名があるのに手順6で区画を捉えていない**なら、間仕切り壁を落としています。

## 手順8. 建具を置く → items

扉と窓を、壁の上に置きます。

- x,y は開口の**中心**。w は開口の幅。
- **向き(rot)は指定しなくて構いません。** アプリが最寄りの壁を見つけて、
  壁に沿った向きに自動で合わせます。中心が正しい壁の上（400mm以内）に
  あることだけが大事です。
- 開き戸は図では1/4円の弧で描かれます。弧の中心が吊元、弧の始まりが開口です。
- 玄関ドアは1階に1つだけ。

## 手順9. 水まわりの設備を置く → items

図に描かれている 浴槽・便器・洗面台・流し台 だけを置きます。
x,y は物の中心、rot は度（0で幅が東西を向く）。

**動かせる家具（ベッド・ソファ・食卓・テレビ・棚）は出力しません。**
図面の家具は「その広さに入る例」として描かれているだけで、実際に置く物では
ありません。アプリ側に家具のカタログがあります。

# 6. 使える種類

items の type は次のものだけです。ここに無いものは出力しないでください。

${itemTypeTable()}

「既定」は、図から大きさが読めないときに使ってよい値です。図から読めるなら
そちらを優先してください。
${hint ? `\n# 7. 利用者からの補足\n${hint}\n` : ""}`;
}

// モデルの出力を、アプリが使う形にそろえる。
//
// responseSchema を付けたので **階ごとの入れ子** で返るのが本筋だが、
// 古い平らな形(walls/labels/items が最上位)や、ページごとの配列で
// 返ってくることもある。読み取りをやり直させるより、受けられる形は
// 受けるほうが安い。
export function decodeCompactPlan(parsed) {
  const out = { walls: [], rooms: [], items: [], labels: [], notes: [], dims: [] };
  if (!parsed || typeof parsed !== "object") return out;

  const merge = (one) => {
    out.walls.push(...one.walls);
    out.rooms.push(...one.rooms);
    out.items.push(...one.items);
    out.labels.push(...one.labels);
    out.notes.push(...one.notes);
    out.dims.push(...one.dims);
  };

  // ページごと / 階ごとの配列で返ることがある。
  if (Array.isArray(parsed)) {
    parsed.forEach((page) => merge(decodeCompactPlan(page)));
    return out;
  }

  // 本筋の形: { floors: [ {floor, dims, walls, labels, items}, ... ], notes }
  if (Array.isArray(parsed.floors)) {
    for (const f of parsed.floors) {
      if (!f || typeof f !== "object") continue;
      const floor = Number(f.floor) || 1;
      const one = decodeCompactPlan(f);
      // 階は入れ子の側が持っている。中の1件ずつに配る。
      one.walls.forEach((w) => { if (w.floor == null) w.floor = floor; });
      one.rooms.forEach((r) => { if (r.floor == null) r.floor = floor; });
      one.items.forEach((i) => { if (i.floor == null) i.floor = floor; });
      one.labels.forEach((l) => { if (l.floor == null) l.floor = floor; });
      one.dims.forEach((d) => { if (d.floor == null) d.floor = floor; });
      merge(one);
    }
    for (const n of Array.isArray(parsed.notes) ? parsed.notes : []) out.notes.push(String(n));
    return out;
  }

  const num = (v) => (typeof v === "number" ? v : Number(v));
  for (const w of Array.isArray(parsed.walls) ? parsed.walls : []) {
    if (Array.isArray(w)) out.walls.push({ x1: num(w[0]), y1: num(w[1]), x2: num(w[2]), y2: num(w[3]), thick: num(w[4]), floor: num(w[5]) || 1 });
    else if (w && typeof w === "object") out.walls.push(w);
  }
  for (const r of Array.isArray(parsed.rooms) ? parsed.rooms : []) {
    if (Array.isArray(r)) out.rooms.push({ x: num(r[0]), y: num(r[1]), w: num(r[2]), d: num(r[3]), floor: num(r[4]) || 1, n: r[5] == null ? "" : String(r[5]) });
    else if (r && typeof r === "object") out.rooms.push(r);
  }
  for (const l of Array.isArray(parsed.labels) ? parsed.labels : []) {
    if (l && typeof l === "object") out.labels.push(l);
    else if (Array.isArray(l)) out.labels.push({ text: String(l[0]), x: num(l[1]), y: num(l[2]), floor: num(l[3]) || 1 });
  }
  for (const n of Array.isArray(parsed.notes) ? parsed.notes : []) out.notes.push(String(n));
  for (const it of Array.isArray(parsed.items) ? parsed.items : []) {
    if (Array.isArray(it)) out.items.push({ type: String(it[0]), x: num(it[1]), y: num(it[2]), w: num(it[3]), d: num(it[4]), rot: num(it[5]) || 0, floor: num(it[6]) || 1 });
    else if (it && typeof it === "object") out.items.push(it);
  }
  if (parsed.dims && typeof parsed.dims === "object") out.dims.push(parsed.dims);
  return out;
}
